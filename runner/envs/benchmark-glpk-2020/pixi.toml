# Auto-translated from the old runner/envs/benchmark-glpk-2020.yaml (conda)
# during the conda -> pixi solver-env migration (issue #478 PR5).
[workspace]
name = "benchmark-glpk-2020"
channels = ["conda-forge"]
platforms = ["linux-64"]

[dependencies]
python = ">=3.9"
pip = "*"
psutil = ">=5.9"
requests = ">=2.32"
linopy = ">=0.5.5"
glpk = "==5.0.0"
