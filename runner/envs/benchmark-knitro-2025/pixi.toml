# Auto-translated from the old runner/envs/benchmark-knitro-2025.yaml (conda)
# during the conda -> pixi solver-env migration (issue #478 PR5).
[workspace]
name = "benchmark-knitro-2025"
channels = ["conda-forge"]
platforms = ["linux-64"]

[dependencies]
python = ">=3.12"
pip = "*"
psutil = ">=5.9"
requests = ">=2.32"
linopy = ">=0.5.5"

[pypi-dependencies]
knitro = "==15.1.0"  # last version released in 2025 per PyPI release history
