# Auto-translated from the old runner/envs/benchmark-cbc-2023.yaml (conda)
# during the conda -> pixi solver-env migration (issue #478 PR5).
[workspace]
name = "benchmark-cbc-2023"
channels = ["conda-forge"]
platforms = ["linux-64"]

[dependencies]
python = ">=3.12"
pip = "*"
psutil = ">=5.9"
requests = ">=2.32"
linopy = ">=0.5.5"
coin-or-cbc = "==2.10.11"
highspy = "*"
